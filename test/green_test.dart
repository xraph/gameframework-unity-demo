import 'package:flutter_test/flutter_test.dart';
import 'package:green/green.dart';

void main() {
  test('package exports GameWidget', () {
    // Verify that GameWidget is accessible through this package
    expect(GameWidget, isNotNull);
  });
}
